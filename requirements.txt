############################################################
# 项目运行 / 训练 所需 Python 依赖
# 已根据当前本地验证过的版本进行锁定，确保 Docker 可重复构建。
# 如需升级，建议逐项测试后再调整版本号。
############################################################

# 基础科学计算
numpy==2.3.3
pandas==2.3.3
matplotlib==3.10.7

# 深度学习 (CPU 版本，若需 GPU 可在 Docker 中换基础镜像+重装 torch)
torch==2.4.1
safetensors==0.6.2
huggingface-hub==0.35.3

# 数据/适配
oss2==2.19.1
scikit-learn==1.7.2

# 可选：如果后续引入 transformers 相关功能，取消下一行注释
# transformers==4.44.2

# 其它（根据需要追加）
# tqdm
# rich
